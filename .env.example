# Copy this file to .env and adjust values as needed.
# If provided, the app will use OpenAI for both the LLM and embeddings.
# If omitted/empty, the app will fall back to local HuggingFace models.
OPENAI_API_KEY=

# Optional model overrides (defaults shown)
#OPENAI_EMBED_MODEL=text-embedding-3-small

# --- HuggingFace (fallback) ---
# Used only when OPENAI_API_KEY is empty.
# You may need to install a compatible PyTorch build for your platform.
HF_LLM_MODEL=google/flan-t5-base
HF_EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2

# --- App Directories ---
DATA_DIR=data
INDEX_DIR=data/index
LOG_DIR=logs

